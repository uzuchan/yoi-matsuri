/**
 * お面屋コアロジック(CHOICE 原型 / 屋台#19)。three / react / DOM 非依存の純TS(D-003)。物理なし。
 */
export { MaskSession } from './session'
export type { MaskInput, MaskEvent, MaskState } from './session'
export { MASK_KINDS, MASK_COUNT } from './params'
export type { MaskKind } from './params'
