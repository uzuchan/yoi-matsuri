# ART DIRECTION — 宵祭(よいまつり)

所有者: art-director / 最終更新: 2026-06-14 / 版: 1.4(D-009: 主役の外部glTF形状を許可(色はコードで§2正規化、外部テクスチャは引き続き禁止))

## 1. ビジュアルコンセプト

「提灯の灯りだけが暖かい、青い夏の夜」。低ポリゴンのプロシージャル造形を、暖色光源と濃い夜のフォグで包む。写実ではなく、記憶の中の祭りの絵。

- コントラスト軸: 寒色の夜(背景) × 暖色の灯り(提灯・屋台)。この対比を壊す中間色光源を置かない
- 外部画像・テクスチャアセット禁止。色とマテリアルは必ずコードで §2 パレットに正規化する(外部テクスチャ・画像・PBRマップは引き続き不可)

### §1.1 外部3Dアセット方針(D-009 部分緩和)

executive-producerの裁定により、以下のとおり **主役オブジェクトに限り**外部glTF/GLBのメッシュ形状(geometry)を許可する。

- **形状(geometry)は glTF/GLB 可。色・マテリアルは必ずコードで §2 パレットに正規化**する(外部テクスチャ・画像・PBRマップは引き続き不可)
- **段階導入。対象は主役のみ**(まず店主、次に金魚)。提灯・群衆・参道などの量産/背景要素はプロシージャル既定を維持する(一括モデル化しない)
- **low-polyを保つ**こと。外部取得メッシュはBlenderで減面してから使用する。性能予算(三角形/draw call/FPS)は §6 のまま不変
- **各glTFアセット追加ごとにart-directorが世界観・パレット適合をレビューし承認**する
- KTX2 / Draco / 全面モデル化 / 外部テクスチャは当面見送り(非スコープ)

## 2. カラーパレット(必ずこのコードを使用)

| 用途 | 色 | 備考 |
|---|---|---|
| 夜空(上端) | `#0a0e2e` | 濃紺 |
| 夜空(地平線) | `#1a2348` | グラデーション下端 |
| フォグ | `#141a38` | 密度は§4参照 |
| 提灯の紙(発光) | `#ff9d45` | emissive。輝度は§4 |
| 提灯の赤帯 | `#c0392b` | 上下の枠 |
| 参道の土 | `#3a3148` | 夜に沈んだ土色。純黒禁止 |
| 鳥居 | `#b03a2e` | 朱。夜なので彩度控えめ |
| 屋台の幕 | `#c0392b` / `#f5f0e8` | 紅白ストライプ |
| 水面(金魚すくい) | `#1e4d6b` | 半透明 opacity 0.85 |
| 金魚(赤) | `#e84a30` | 主役。emissive `#3a0f08` を弱く |
| 金魚(白模様) | `#f5f0e8` | |
| 群衆シルエット | `#0d1126` | 無発光。下記の明度持ち上げ例外を許容 |
| プレイヤー胴・頭 | `#1b2240` | 寒色。群衆より一段明るい固定の床値(下記プレイヤー視認性参照) |
| プレイヤー暖色アクセント帯 | `#ff9d45` | 提灯紙と同色。emissive 弱。面積は胴の上部・細帯に限定 |
| 出目金(黒) | `#1a1a22` | 報酬演出用 |
| ポイの枠 | `#e8c87a` | プラスチック風 |
| ポイの紙 | `#f5f0e8` | 耐久低下で opacity 0.95→0.4 |
| 花火(基本3色) | `#ff6b9d` `#ffd166` `#4ecdc4` | 加算合成 |
| UIテキスト | `#f5f0e8` | 背景 `#0a0e2e` の80%透過パネル |
| UIアクセント | `#ff9d45` | 選択中・強調 |

**群衆シルエットの明度持ち上げ例外(夜の前景での背景分離)**: 群衆シルエットの基準色は `#0d1126`。ただし夜の前景地面(土 `#3a3148` が暗く沈んだ領域)と明度が拮抗し、群衆が背景へ埋没して「人がいる」と読めなくなる場合に限り、フォグ色 `#141a38` を**上限**として明度のみを持ち上げ、背景から最小限分離してよい。このとき色相は寒色のまま据え置き、新規の色相を持ち込まない(`#141a38` は本パレットのフォグ色であり、新色の追加には当たらない)。基準色 `#0d1126` を超えて明るくする根拠は背景分離のみで、`#141a38` を超えてはならない(T-002 ループ2で適用)。

**金魚すくいシーンの器・背景・紙劣化の明確化(T-006 で確定)**: §2 表で水面・金魚・ポイの色は定義済みだが、(a)水槽の器(縁・壁・底)の色、(b)シーン背景、(c)紙の段階劣化の境界が未定義だった。以下に数値で固定する(いずれも §2 既存パレット内。新色相は持ち込まない)。

- **水槽の器(縁/壁/底)**: 縁は参道の土と同じ `#3a3148`(夜に沈んだ器として参道と共通の寒色)。鉢の側壁・底は `#3a3148` を 0.6 倍した暗色(縁より沈めて深さを示す)、水の底面は水面色 `#1e4d6b` を 0.4 倍した暗色。器は無発光(被ライティングのみ)で、夜祭りの寒色トーンを保つ。
- **水面の弱発光**: 水面 `#1e4d6b` op0.85 は、夜のフォグ背景に沈んで「黒い穴」に見えるのを防ぐため emissive `#1e4d6b` を emissiveIntensity 0.18 で弱く重ねてよい(器の中の水が淡く光る)。色相は水面色のまま。輝度は屋台前(§7-4)・金魚(主役)を超えてはならない。
- **シーン背景**: goldfish シーンは俯瞰のため空グラデーション(§2 夜空)は画面に入らない。背景はフォグ色 `#141a38` の単色とし、approach の夜のトーン(フォグ色は両シーン共通)と地続きに見せる。フォグは近接俯瞰スケールに合わせ `FogExp2(0x141a38, 0.12)` を許容する(§4 の参道用 0.028 はワールドスケール用。goldfish は水槽が camera から約1.5mの近接スケールのため密度を上げて器の縁を背景へ自然に馴染ませる。色は §4 と同一 `#141a38`)。
- **ポイ紙の段階劣化(GDD §4.6 破損予兆)**: 紙 `#f5f0e8` の不透明度は耐久比 r=durabilityRatio に対し基本 `op = 0.4 + 0.55·r`(r=1で0.95、r=0で0.4)。残30以下(r≤0.3)では破損予兆として透けをさらに強め、係数 `(0.55 + 0.45·(r/0.3))` を上記に乗じる(r=0.3で×1.0=境界連続、r=0で×0.55)。結果として r=0 付近では実効 op ≈ 0.22 まで透ける。HUD ゲージは r≤0.3 で警告色(金魚赤 `#e84a30`)+点滅に切り替える(§2 UIアクセント運用と別に、警告は金魚赤を流用してよい)。

**プレイヤーの視認性(T-003 で確定)**: プレイヤーは三人称追従カメラ(§5 approach)で常時画面に映る操作対象であり、群衆シルエットより**読みやすく**なければならない(GDD「三人称見下ろし気味の追従カメラ。プレイヤーは参道を歩く」)。問題: T-003 実装は胴を `MeshLambertMaterial`(被ライティング)で作ったため、入口や屋台横の影域では入射光が乏しく胴が純黒近くまで沈み、暖色帯のみが頼りになっていた(群衆=base色を持ち上げた**無発光**より暗く見える逆転)。
方針:
1. プレイヤー胴・頭は**寒色の固定床値 `#1b2240`(luma≈30)を保証する**。群衆の上限フォグ色 `#141a38`(luma≈27)より一段明るく、影域でもシルエットが読める。実装は無発光のbase色(`MeshBasicMaterial` 相当)か、`emissive #1b2240 / emissiveIntensity 1.0` を base に重ね、照明域では加えて明るくなる形のいずれか。**胴が照明の有無で純黒へ沈まないこと**が要件。
2. 暖色アクセント帯は祭りの法被/手ぬぐいの示唆として**残すが増やさない**。色 `#ff9d45`、emissiveIntensity ≤0.35、面積は胴の上部の細帯1本のみ。帯がプレイヤーの主たる視認手段になってはならない(§7-4 の屋台前=最暖・最明の視線誘導と競合させないため、暖色面積は屋台・提灯より常に小さく保つ)。
3. プレイヤーの暖色がシーンで一番明るい暖色になってはならない。最暖・最明は屋台前(§7-4)。プレイヤーの帯は「いる」と読める最小限で足りる。

## 3. 造形仕様(プロシージャル。単位: m)

- **提灯**: 球を上下に潰した楕円体(径0.35、高さ0.45)+上下に円筒の枠。参道両脇に2.5m間隔で各側24個、高さ2.6mのワイヤーに吊る。各提灯はわずかに揺れる(±2°、周期3〜5sでランダム位相)
- **鳥居**: 参道終端(z=-60付近)に高さ8m。シルエット重視、ディテール不要。**焦点としての発色(磨き 2026-06-18 で確定)**: 鳥居はカメラから約69m先で、§4 の `FogExp2(密度0.028)` では色の約97%が fog 色 `#141a38` に置換され、無発光だと参道の象徴(視線の終点)が黒へ沈んで死ぬ。提灯・裸電球と同じ「夜でも読める光」の扱いとして、**鳥居マテリアルのみ fog を無効化(`fog:false`)**し、靄を抜けて浮かぶ朱の門として読ませる。発色は朱 `#b03a2e` の base に同色 `emissive` を `emissiveIntensity 0.6` で重ねる(ネオン化を避ける弱め。プレイヤー胴 1.0 / 店主胴 0.9 より低い)。新色相は持ち込まない(§2 の `#b03a2e` のまま)。**判定基準**: 参道正面の構図で鳥居が朱の門として識別でき、かつ平板なベタ塗りの一様発光に見えないこと
- **金魚すくい屋台**: 間口3m×奥行2m。紅白幕、カウンター、屋根、裸電球2個(`#ffd166`)、水槽(楕円)。店主は単純化した人型シルエット+前掛け
- **群衆シルエット**: 単純化人型(高さ1.5〜1.8m)を参道脇に15〜20体。`#0d1126` の無発光マテリアル(夜の前景での背景分離は §2 の明度持ち上げ例外で上限フォグ色 `#141a38` まで許容)。ゆっくり揺れる(歩行はVertical Sliceでは不要)。**揺れの数値(T-009 で確定)**: 各個体は足元(y=0)を支点に前後へ ±3°(±数度)、周期4〜7s、index ベースの決定論的位相で正弦揺らす。提灯(±2°/周期3〜5s)よりわずかに大きく遅い「立っている人の重心移動」とし、静止に見せない(§7-3)。歩行ボブ・移動は付けない(立ち止まった群衆)
- **プレイヤー**: 単純化人型・身長 ~1.7m。胴=カプセル(半径0.22、円柱部1.0)+頭=球(半径0.17)、足元 y=0。胴・頭は寒色 `#1b2240`(§2 視認性方針: 照明の有無で純黒へ沈ませない床値を保証)。暖色アクセント帯=胴上部に細帯1本のみ(`#ff9d45`、emissiveIntensity ≤0.35、半径は胴+0.015、高さ0.12)。歩行アニメは本スライス範囲外(静的造形のみ)。新規色相は持ち込まない(全て §2 内)
- **金魚**: 紡錘形ボディ+三角尾びれ(計3メッシュ程度)。泳ぎに合わせ尾を±20°振る
- **花火**: パーティクル(1発120〜200粒、加算合成、重力落下+減衰、寿命1.8s)。打ち上げ筋→開花→残光の3段階。

  **花火の見栄え数値(T-009 で確定)**: 上記の段数・粒数・寿命に加え、夜空での発色と白飽和の扱いを以下に固定する(すべて §2 花火3色 `#ff6b9d`/`#ffd166`/`#4ecdc4` の範囲内。新色相は持ち込まない)。
  - **粒サイズ**: `PointsMaterial.size = 0.6`(sizeAttenuation。開花点 z≈-42〜-52m の遠方でも個々の火花の**色相**が読める大きさ。**F-002 で 0.45→0.6 へ拡大**。0.45 では遠方で点がアンチエイリアスにより白く溶け、色が読めなかった)。
  - **加算合成の白飽和対策**: 開花の輝度ピークは `alpha ≤ 0.7`(**F-002 で 0.85→0.7 へ引き下げ**。1.0 にしない)。開花直後は粒が開花点近傍に密集して加算合成で中心が白へ飽和し色相が消えるため、ピークを下げて密集時の白飽和を抑える。粒サイズ拡大(0.6)による重なり増は、このピーク引き下げで相殺する。**判定基準**: 開花の塊が一様な白い球に見えてはならない。中心はやや明るくとも、外周の火花で `#ff6b9d`/`#ffd166`/`#4ecdc4` のいずれかの色相が識別できること(**F-002 解消・磨き 2026-06-18**: size 0.6 + alpha 0.7 で実機スクショにて開花の色相=金 `#ffd166` 等が読めることを確認。reports/screenshots/POLISH-burst-*.png の before/after)。
  - **残光フェード**: 寿命の前半45%は輝度ピーク維持、後半55%で線形に 0 へ。打ち上げ筋(launch)は burst 点の下へ約3mの細い尾とし、開花とつながった1発に見せる。
  - **開花点(視認性)**: approach カメラ(高さ3.2m・俯角15°・FOV55°)で画面上部の夜空に見えるよう、高さ10〜14m(鳥居 y=8 の上)・奥行 z≈-42〜-52m(鳥居の向こう)・x∈±10m に散らす。ライトは増やさない(加算合成の Points のみで夜空に映す。§6 の PointLight 6灯予算を消費しない)。
  - **打ち上げ間隔**: 30〜45s(GDD §2)。VS デモ視認性のため初回のみ早め(〜10s)。提灯・屋台の暖色が常時の主役で、花火は夜空のアクセント(常時連発しない)。

## 4. ライティング・フォグ・ポスト

| 項目 | 仕様 |
|---|---|
| 環境光 | HemisphereLight: 空 `#2a3360` / 地 `#1a1430`、intensity 0.5 |
| 提灯光 | PointLightは**4灯まで**(近傍の代表位置に配置)。残りの提灯はemissiveのみで光って見せる。color `#ff9d45`, intensity 1.2, distance 6, decay 2 |
| 屋台光 | PointLight 1灯 `#ffd166`, intensity 1.5, distance 8(屋台前で最も明るい場所を作る) |
| フォグ | `THREE.FogExp2(0x141a38, 0.028)` |
| トーン | `ACESFilmicToneMapping`, exposure 1.1 |
| 影 | 影マップは**使用しない**(性能予算)。接地感は暗色の接地円で代用 |

**縁日屋台群の発光方針(T-012 で確定)**: 参道両脇の装飾屋台(非インタラクティブ)は PointLight を追加しない(動的PointLight は §4/§6 の6灯上限を超えない)。屋台の灯りは裸電球の emissive のみで表現する。屋根・幕には「PointLight の届かない遠方でも純黒に沈まない」ための弱い emissive 床を付与してよい。数値上限は以下のとおり(いずれも裸電球の emissiveIntensity 1.6 より十分低く、§7-4「屋台前=最明」を崩さないこと):

| 部位 | emissive カラー | emissiveIntensity 上限 |
|---|---|---|
| 屋根 | 幕赤 `#c0392b`(そのまま) | 0.30 |
| 幕(赤短冊) | 幕赤 `#c0392b`(そのまま) | 0.30 |
| 幕(白短冊) | 幕白 `#f5f0e8`(そのまま) | 0.25 |
| 裸電球 | 電球色 `#ffd166`(そのまま) | 1.6(変更禁止) |

emissive カラーは §2 パレット既定色のまま使用し、新色相を持ち込まない。上限を超えて上げると近景の金魚すくい屋台(§7-4 最明)との輝度差が縮まり視線誘導が薄れるため、超えてはならない。

**縁日屋台の屋号配色原則(T-012 で確定)**: 屋号看板は「白地×赤字」(背景 `#f5f0e8`、文字 `#c0392b`、縁取り `#c0392b`)を基本とする。これは日本の縁日看板の慣用であり、本パレット内の既存色を流用している。屋台ごとの「多彩さ」は屋号・暖簾(背板)の差し色・陳列品の形で出し、看板の地色・文字色は変えない(変えると他の屋台と区別がつかなくなる = 単調化を防ぐ手段は色ではなく文字・差し色・形)。差し色として使ってよい色は FESTIVAL_ACCENTS(§2 パレット内の再利用色)のみ。新色相を持ち込まない。

## 5. カメラ

- approach: プレイヤー後方5m・高さ3.2m・俯角15°の追従カメラ(追従lag 0.15s)。FOV 55°
- goldfish: 水槽真上やや手前から俯角70°固定。水槽が画面の70%を占める
- result: 屋台正面、店主が中央に入る固定カメラ

**result カメラの確定(T-007 で裁定 — 背景再利用の乖離を却下し専用カメラを要求)**: T-007 実装は背景に ApproachScene.render を再利用し(終了直前のプレイヤー追従カメラ位置で固定)、屋台が右端・店主が画面外という構図になっていた(reports/screenshots/T-007-result.png)。これは §5 result「屋台正面、店主が中央」と乖離し、かつ PRODUCT_VISION 体験目標3「小さな物語=店主の反応と報酬で『屋台で遊んだ』記憶を作る」の余韻を損なう(店主のセリフが出ているのに当の店主が画面にいない)。したがって **背景再利用は許容せず(a を却下)、result 専用の固定カメラを要求する(b を採択)**。世界(world)は ApproachScene 所有のまま再利用してよいが、描画に使うカメラのみ result 専用へ差し替える。専用カメラの数値仕様を以下に固定する(approach カメラとは別インスタンス。result シーン中のみ使用):

  - **注視点(lookAt)**: 屋台の店主の頭部付近。屋台中心を世界座標 S=(stall.x, stall.y, stall.z) とし、注視点は S を基準に y=+1.5m(店主の頭の高さ)。店主が画面横方向の中央(±5% 以内)に来ること。
  - **カメラ位置**: 屋台の正面(参道側=店主が客と対面する向き)から、屋台中心へ向かって水平距離 4.5m・高さ 1.8m。俯角は注視点(店主頭部 y=1.5)へ向けて約 4〜8°(ほぼ水平、店主とわずかに見上げる客の目線)。屋台の「正面」とは店主の前掛けが見える側(approach でプレイヤーが屋台に対面した向き)であり、屋台側面・背面から撮らない。
  - **FOV**: 50°(approach の 55° よりやや寄り、屋台+店主+裸電球2個が画面に収まり、店主が画面縦の中央〜やや上に来る)。
  - **構図要件**: 画面内に (1)店主シルエット+前掛けが中央、(2)屋台の紅白幕と裸電球2個(§4 屋台光が画面内で最も明るい=§7-4 を結果画面でも満たす)、(3)背景に提灯列が最低2個以上(寒色の夜×暖色の灯りの対比 §7-1 を保つ)。鳥居は任意。結果パネル(画面中央の80%透過)が店主の上半身に被るのは可だが、店主の頭〜肩が左右どちらかにはみ出して読めること。
  - **固定**: result 中はカメラを動かさない(追従・揺れなし)。プレイヤー造形は画面に入っても入らなくてもよい(主役は店主)。

  担当への差し戻し(4点セット): 対象オブジェクト=result シーンの背景描画カメラ(src/scenes/result/ResultScene.ts が ApproachScene.render を再利用している箇所)/ 現状値=approach 追従カメラ(プレイヤー後方5m・高さ3.2m・俯角15°・FOV55°、終了直前位置で固定。屋台が右端・店主が画面外)/ 期待値=上記 result 専用固定カメラ(屋台正面・水平距離4.5m・高さ1.8m・俯角約4〜8°・FOV50°、店主頭部 y=1.5 を注視・店主が画面横中央±5%)/ 該当仕様=ART §5 result・§7-1・§7-4、PRODUCT_VISION 体験目標3。実装はワールド再利用のまま専用 Camera を1つ追加し render で差し替える方針でよい(world 二重生成は不要 / 性能予算 §6 を増やさない)。

**result カメラの「正面」の確定(T-007 ループ2 で裁定 — 前掛け向きの矛盾を解消)**: 上の差し戻しを gameplay-engineer が解消し(ResultScene に result 専用固定カメラを追加し renderWith で描画)、reports/screenshots/T-007-result.png で屋台が画面中央へ来た(屋台が右端・店主が画面外という却下理由は解消)。実装報告で「ART §5 は『正面=前掛けが見える側』と記すが、既存 world 造形(src/world/stall.ts、group rotation.y=-90°)では前掛け(apron)・紅白幕の正面が参道と逆向き(world +x 側)を向く一方、プレイヤーが屋台に対面した側・屋台 PointLight(§7-4 屋台前を最も明るく)はいずれも world -x 側にある。両立しないため -x 側カメラを採用した」との確認があった。座標を裏取りしたところ事実と一致する(紅白幕の正面 world x≈6.3、apron 正面は +x 向き / 店主 world≈(4.5,-,-25.7)、裸電球 world≈(5,2.1,-26.9)・(5,2.1,-25.1)、屋台 PointLight world≈(4,-,-26))。

裁定: **-x 側カメラを許容する(前掛け向きの是正は要求しない)。** §5 の「正面=前掛けが見える側」の語は撤回し、以後 result の「正面」は (1)approach でプレイヤーが屋台に対面した側=-x 側、(2)§7-4 屋台前(裸電球・屋台 PointLight)が画面内で最も明るくなる側、の2点で定義する。前掛けの面を是正してカメラ側へ向ける利得は小さい — 店主は §3 通り無発光の暗色シルエット(`#0d1126` / MeshBasicMaterial)であり、前掛けの面はどの角度からも「前掛け」として読めない。一方 +x 側へ撮ると §7-4(最明=屋台前)と approach 継続性を同時に失う。よって VS 品質としては「店主シルエットが画面中央付近に見える」ことで十分とし、前掛けの面の可読性は要件にしない。environment-engineer への是正依頼は出さない。

T-007 ループ2 残課題(Minor / result カメラ解消の妨げにはしない): 店主シルエット(near-black `#0d1126`)が中央の 80% 透過パネル背後に隠れ、かつ夜の暗背景と明度が拮抗して「頭〜肩が左右へはみ出して読める」(§5 構図要件3)が現状成立していない。店主は意図的な暗色シルエットで余韻は本文テキスト(店主ラベル+セリフ)が担うため再差し戻し(不合格)はしないが、フォローとして次のいずれかを推奨する: (a)result 専用カメラの注視点を屋台中心ではなく店主頭部 world≈(4.5,1.66,-25.7)へ寄せ、店主をパネルの片側へ約 5〜10% ずらして頭〜肩を露出させる、または (b)store keeper シルエットに §2 群衆の明度持ち上げ例外と同等の背景分離(上限フォグ色 `#141a38`)を適用し、暗背景から最小限分離する。担当=gameplay-engineer((a)は ResultScene のカメラ注視点)/ environment-engineer((b)は src/world/stall.ts の keeperMaterial)。VS 範囲では任意。

**T-007 ループ2 残課題の解消(T-009 で確認 — 推奨案 a を採用)**: gameplay-engineer が推奨案(a)を実装した。result 専用カメラの注視点を店主頭部 X(4.5)・高さ1.5へ寄せ、カメラ rig 全体を world +z へ `RESULT_KEEPER_PAN_Z = 1.8m` パンして店主を画面中央から左へ約23%(画面 x≈23%)ずらした(中央パネル左端 x≈28% の左外に頭〜肩が出る)。パン後も裸電球2個・屋台前(§7-4 最明)は画面内に残り、approach 継続性も維持される。reports/screenshots/T-007-result.png(2026-06-14 撮影、ResultScene の T-009 改修後)で確認: 屋台・紅白幕・裸電球2個が画面左に最明として残り(§7-4 ✓)、結果パネルが中央に被っても店主側が左外へ寄り、画面右下にはプレイヤー造形(寒色胴+暖色帯)が入る構図になっている。**店主シルエット自体は依然 near-black `#0d1126`(被ライティング無しのベース色)で暗背景に対し可読性が低い**(店主の前掛け apron は MeshLambertMaterial 白だが world +x 側を向くため -x カメラからは見えない)。VS としては「店主側が中央パネルの外へずれ、店主の位置が画面内に確保された(余韻は本文テキストが担う)」ことで §5 構図要件を満たすと判定する。前掛けの面の可読性・店主シルエットの明度持ち上げ(推奨案 b)は要件にしない(T-007 ループ2 裁定どおり)。Minor はクローズ。

**F-004 の解消(2026-06-18 ビジュアル磨き — 推奨案 b を追加採用)**: T-009 で案(a)カメラオフセットを入れてもなお、店主シルエットは near-black `#0d1126`(`MeshBasicMaterial`・無発光)のため暗背景に対し可読性が低く、本作の感情的ペイオフ(「店主の反応」)が画面で読めない状態が残っていた(BACKLOG F-004)。そこで推奨案(b)を実装する: 店主の胴・頭を**プレイヤー胴と同じ寒色の床値 `#1b2240`** へ変更し(店主も人型)、同色 `emissive`(emissiveIntensity 0.9)を重ねて、屋台 PointLight が背面に回りにくい結果画面でも純黒へ沈ませない(ART §2「照明の有無で純黒へ沈ませない床値」と同方式)。実装は `src/world/stall.ts` の `keeperMaterial` を `MeshBasicMaterial #0d1126` → `MeshStandardMaterial(color/emissive #1b2240, intensity 0.9)`。これは D-009 店主 glTF の body 正規化(`OWNER_PALETTE_SPECS.body` = `#1b2240`/emissive 0.9)とも一致し、glTF 差し替え前後で店主の明度が揃う。前掛け(`#f5f0e8`)は被ライティングで屋台光の暖色を受け、寒色胴との明暗で店主が読める。新色相は持ち込まない(`#1b2240` は §2 既定のプレイヤー胴色)。result だけでなく approach(屋台に立つ店主)・dialogue でも店主の視認性が上がる。reports/screenshots/F004-result-{before,after}.png で確認。Minor/F-004 は解消。

## 6. 性能との取り決め(TECHNICAL_ARCHITECTUREと連動)

- 動的PointLightは合計6灯以下。ジオメトリはシーン全体で50k三角形以下
- 提灯・群衆はInstancedMesh必須
- この予算内で50 FPSを維持できない演出は削る(美しさより滑らかさ)

## 7. レビュー基準(art-directorが視覚レビューで確認する項目)

1. 寒色の夜と暖色の灯りの対比が一目で成立しているか
2. パレット外の色が画面に存在しないか
3. 提灯の揺れ・金魚の尾・水面など「静止していない」要素が常に画面にあるか
4. 屋台前がシーン中で最も明るく、自然に視線誘導されるか

## 8. お面造形原則(P2 お面屋 / 屋台#19 / mask:* 種別ごとの造形ガイド)

**確定日**: 2026-06-14(P2視覚レビュー差し戻し基準として制定)。
**背景**: T-P2-mask.png レビューで5種全てが「円盤+左右対称2点目+口弧」のスマイル記号に見え、「日本の縁日のお面」として識別不能と判定。種別ごとの造形要素を以下に数値で定義する。プロシージャル(Three.js ジオメトリ)で実現できる範囲。色はすべて ART §2 パレット内。新色相持ち込み禁止。

**共通ルール**:
- `browGeo`(現行の `RingGeometry` 弧)は**全種共通で廃止する**。現在スクリーンショットで顔の中央付近に「3つ目の点」として読まれており視覚ノイズになっている。種別ごとに目的を持った眉ジオメトリで置き換える
- 口の `mouthGeo`(現行 `CircleGeometry(MASK_RADIUS * 0.12, 16)` 中央配置)は種別ごとに位置・形・サイズを変える。「全員が中央下に同じ丸口」を廃止する
- 目の `eyeGeo`(現行 `CircleGeometry(MASK_RADIUS * 0.13, 16)` 左右対称)は種別ごとに形・縦横比・左右対称性を変える。「全員が同サイズの2点」を廃止する

### 8-1. キツネ (mask:kitsune / `#f5f0e8` 顔 / `#c0392b` 差し色)

キツネの最小記号は「耳の三角シルエット」と「ツリ目」。これがなければキツネとして読めない。

| 部位 | 実装ガイド | 数値 |
|---|---|---|
| 耳(左右) | `ShapeGeometry` 三角形2枚。顔の後ろ(z=-0.01)に配置してシルエットに出す | 位置 `x = ±MASK_RADIUS * 0.68, y = MASK_RADIUS * 0.82`、高さ `MASK_RADIUS * 0.55`、底辺 `MASK_RADIUS * 0.35`、色=顔色 `#f5f0e8` |
| 内耳 | 耳より小さい三角(上に重ねる) | 高さ 0.65倍、底辺 0.65倍、色=`#c0392b`、z=+0.005 |
| 目 | `CircleGeometry(MASK_RADIUS * 0.10, 16)` を scale.x=2.0 で横楕円化。ツリ目感のため上端をやや持ち上げる | 位置 `x = ±MASK_RADIUS * 0.34, y = MASK_RADIUS * 0.22`。色=`#c0392b` |
| 鼻 | 小さな逆三角形 `ShapeGeometry` | 中央 `y = 0.0`、幅 `MASK_RADIUS * 0.14`、高さ `MASK_RADIUS * 0.10`、色=`#c0392b` |
| 口 | 細い弧 `RingGeometry` のアーク角を絞る(現行 0.8π→0.5π) | 中央下 `y = -MASK_RADIUS * 0.42`、色=`#c0392b` |
| 眉 | 廃止(browGeo 不使用) | — |

### 8-2. ひょっとこ (mask:hyottoko / `#ff9d45` 顔 / `#c0392b` 差し色)

ひょっとこの最小記号は「左寄りに偏った楕円口」と「左右非対称の目」。

| 部位 | 実装ガイド | 数値 |
|---|---|---|
| 口(最重要) | `CircleGeometry(MASK_RADIUS * 0.20, 16)` を scale.x=0.85 / scale.y=1.0 の楕円にし、**左寄り**に配置。これがひょっとこの記号 | 位置 `x = -MASK_RADIUS * 0.22, y = -MASK_RADIUS * 0.30`。色=`#c0392b` |
| 目(左) | 通常サイズ | `CircleGeometry(MASK_RADIUS * 0.13, 16)` / `x = -MASK_RADIUS * 0.34, y = MASK_RADIUS * 0.18` |
| 目(右) | 細め(左より小さく+右目を閉じ気味) | `CircleGeometry(MASK_RADIUS * 0.08, 16)` を scale.y=0.5 で横扁平 / `x = MASK_RADIUS * 0.34, y = MASK_RADIUS * 0.15` |
| 眉(左) | `PlaneGeometry(MASK_RADIUS * 0.25, MASK_RADIUS * 0.06)` / `rotation.z = 0.15rad`(外下がり) | `x = -MASK_RADIUS * 0.34, y = MASK_RADIUS * 0.36`。色=`#c0392b` |
| 眉(右) | 同上 / `rotation.z = -0.35rad`(より急な外下がり) | `x = MASK_RADIUS * 0.34, y = MASK_RADIUS * 0.33`。色=`#c0392b` |
| 顔外形 | scale.y=0.92 で縦に少し扁平 | — |

### 8-3. おかめ (mask:okame / `#f5f0e8` 顔 / `#ff6b9d` 差し色)

おかめの最小記号は「大きな丸頬」。桃色の丸2枚が左右対称に並んでいれば「おかめ」と読める。

| 部位 | 実装ガイド | 数値 |
|---|---|---|
| 頬(最重要・左右) | `CircleGeometry(MASK_RADIUS * 0.28, 16)` を左右に配置。桃色で無発光 | 位置 `x = ±MASK_RADIUS * 0.60, y = -MASK_RADIUS * 0.10`。色=`#ff6b9d`、emissive なし |
| 目 | 小さめの2点(現行より径を 0.85倍) | `CircleGeometry(MASK_RADIUS * 0.11, 16)` / `x = ±MASK_RADIUS * 0.30, y = MASK_RADIUS * 0.20`。色=`#c0392b` |
| 鼻 | 極小の正円 | `CircleGeometry(MASK_RADIUS * 0.07, 12)`、中央 `y = MASK_RADIUS * 0.05`。色=`#f5f0e8`(顔色より僅かに暗い `#e0dbd0`) |
| 口 | 小さな弧。現行の「3つ目の点」になっている口を**下へ下げ**アーク角を絞る | 中央 `y = -MASK_RADIUS * 0.48`、アーク角 0.45π。色=`#ff6b9d` |
| 眉 | 短い `PlaneGeometry` を水平に近い角度で配置(おかめは眉が短く額の中心寄り) | `PlaneGeometry(MASK_RADIUS * 0.18, MASK_RADIUS * 0.05)` / `x = ±MASK_RADIUS * 0.25, y = MASK_RADIUS * 0.40`。色=`#c0392b` |

### 8-4. 般若 (mask:hannya / `#b03a2e` 顔 / `#1a1a22` 差し色)

般若の最小記号は「額の角2本」と「逆ハの字の眉」と「白い牙」。このうち**角**がなければ般若と認識されない。

| 部位 | 実装ガイド | 数値 |
|---|---|---|
| 角(最重要・左右) | `ShapeGeometry` 三角形2本。顔から上に突き出るようにシルエットへ出す(z=-0.01、顔の後ろ) | 位置 `x = ±MASK_RADIUS * 0.48, y = MASK_RADIUS * 0.80`、高さ `MASK_RADIUS * 0.45`、底辺 `MASK_RADIUS * 0.22`、内傾き `rotation.z = ∓0.15rad`。色=`#1a1a22` |
| 眉(逆ハの字) | `PlaneGeometry(MASK_RADIUS * 0.32, MASK_RADIUS * 0.08)` を眉間に寄せ急傾斜で配置 | `x = ±MASK_RADIUS * 0.28, y = MASK_RADIUS * 0.30`、`rotation.z = ±0.50rad`。色=`#1a1a22` |
| 目 | 横長扁平(横長三角目の代替) | `CircleGeometry(MASK_RADIUS * 0.10, 16)` を scale.x=2.5 / scale.y=0.45 で横扁平。`x = ±MASK_RADIUS * 0.34, y = MASK_RADIUS * 0.12`。色=`#1a1a22` |
| 口 | 広い口弧(口角上がり=「怒り笑い」に見せるため弧を広く) | `RingGeometry(MASK_RADIUS * 0.40, MASK_RADIUS * 0.50, 24, 1, -0.25π, 1.50π)` / `y = -MASK_RADIUS * 0.26`。色=`#1a1a22` |
| 牙(左右) | 顔下端から突き出る白い三角2本 | `ShapeGeometry` 三角 / `x = ±MASK_RADIUS * 0.22, y = -MASK_RADIUS * 0.88`、高さ `MASK_RADIUS * 0.14`、底辺 `MASK_RADIUS * 0.11`。色=`#f5f0e8` |
| 鼻 | 低く横長の小形 | `CircleGeometry(MASK_RADIUS * 0.08, 12)` scale.x=1.5 / 中央 `y = 0`。色=`#1a1a22` |

### 8-5. アニメ風 (mask:anime / `#ffd166` 顔 / `#4ecdc4` 差し色)

アニメ風の最小記号は「横長の大きな瞳」と「V字の笑顔口」。現代の縁日らしさを色と目の形で出す。

| 部位 | 実装ガイド | 数値 |
|---|---|---|
| 目(最重要) | `CircleGeometry(MASK_RADIUS * 0.14, 20)` を scale.x=2.0 / scale.y=0.9 の大きめ横楕円 | `x = ±MASK_RADIUS * 0.30, y = MASK_RADIUS * 0.16`。色=`#4ecdc4` |
| 目の睫毛ライン | 目の上端に `PlaneGeometry(MASK_RADIUS * 0.30, MASK_RADIUS * 0.030)` の細帯 | `x = ±MASK_RADIUS * 0.30, y = MASK_RADIUS * 0.26`。色=`#1a1a22`(暗色) |
| 口 | `ShapeGeometry` で逆V字(笑顔) — 中央上から左右下へ折れる2辺を `LineCurve` で構成、厚さをもたせる | 頂点 `(0, -MASK_RADIUS * 0.20)` から `(±MASK_RADIUS * 0.35, -MASK_RADIUS * 0.42)` へ折れる、ライン幅 `MASK_RADIUS * 0.05`。色=`#4ecdc4` |
| 照れ線 | 目の下の横線2本(アニメ特有の表現) | `PlaneGeometry(MASK_RADIUS * 0.22, MASK_RADIUS * 0.025)` / `x = ±MASK_RADIUS * 0.30, y = MASK_RADIUS * 0.00`。色=`#4ecdc4`、透過 opacity=0.6 |
| 眉 | `PlaneGeometry` の短い帯を平行に配置(アニメらしい太眉) | `PlaneGeometry(MASK_RADIUS * 0.24, MASK_RADIUS * 0.07)` / `x = ±MASK_RADIUS * 0.30, y = MASK_RADIUS * 0.38`。色=`#1a1a22` |
| 顔外形 | scale.y=1.08 で縦に少し伸ばす(タマゴ型輪郭) | — |

### 8-6. 実装上の制約まとめ

- 角・耳の `ShapeGeometry` は `z=-0.01`(顔の裏に重ねてシルエットに突き出す)で、専用ジオメトリを `track()` して dispose 対象に登録すること
- 全ジオメトリは種別ごとに固有(共有不可。形が異なる)
- 色はすべて §2 パレット内(`#f5f0e8` / `#c0392b` / `#ff9d45` / `#ff6b9d` / `#ffd166` / `#4ecdc4` / `#b03a2e` / `#1a1a22`)。新色相を持ち込まない
- `browGeo` は廃止。`MaskScene.ts` の `browGeo` 宣言と各 `brow = new Mesh(...)` を全種削除した上で、種別ごとの眉を追加する
- 種別判定は `kind.id` の switch/if で分岐してジオメトリを差し替える(共通 faceGeo + eyeGeo + mouthGeo の外側に種別固有パーツを追加する形でもよい)
